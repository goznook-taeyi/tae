# Web
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Data models / config
pydantic>=2.7
pydantic-settings>=2.2

# Video / imaging
opencv-python-headless>=4.9
numpy>=1.26
Pillow>=10.3

# OCR (default engine). PaddleOCR pulls paddlepaddle (large, CPU wheel).
# Imported lazily by app.ocr.PaddleOCREngine so the app/tests run without it.
paddleocr>=2.7
paddlepaddle>=2.6

# Spell check + similarity + HTTP (hanspell / CLOVA)
py-hanspell @ git+https://github.com/ssut/py-hanspell.git@471809d55f9d3d2b843b4d770f5d0e9280e8be3d
python-Levenshtein>=0.25
requests>=2.31

# --- dev / test ---
pytest>=8.2
httpx>=0.27
